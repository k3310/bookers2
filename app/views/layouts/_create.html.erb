	  <div class='col-md-3'>
	    <h2>User info</h2>
	    <!-- モックのため、固定値や未実装になっている部分があります。アプリ完成前に修正しましょう。 -->
	    <table class='table'>
	      <tr>
	        <%= image_tag user.get_profile_image(100,100) %>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	      <tr>
	        <th>name</th>
	        <th><%= user.name %></th>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	      <tr>
	        <th>introduction</th>
	        <th><%= user.introduction %></th>  <!-- 固定値になっているので変数を使いましょう -->
	      </tr>
	    </table>
	    <div class='row'>
			<%= link_to edit_user_path(user), class: "col-12 btn btn-outline-secondary" do %>
				<i class="fas fa-user-cog"></i>
			<% end %>
	    </div>
	    <h2 class="mt-3">New book</h2>
	    <!-- form_withを用いて新規投稿フォームを作成しましょう。 -->
			<%= form_with model: book do |f| %>
				<div class="form-group">
				  <%= f.label :title %><br />
				  <%= f.text_field :title, class: "form-control", autofocus: true, autocomplete: "title" %>
				</div>
				<div class="form-group">
				  <%= f.label :opinion %><br />
          <%= f.text_area :body, class: "form-control", autofocus: true %>
				</div>
				<div class="form-group">
					<%= f.submit "Create Book", class: "btn btn-sm btn-success" %>
				</div>	
			<% end %>
	  </div>